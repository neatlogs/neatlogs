# Example 49: LangGraph + Gemini + Qdrant RAG + Cohere Reranker
# ==============================================================
# Required env vars (.env):
#   NEATLOGS_API_KEY, NEATLOGS_ENDPOINT
#   GEMINI_API_KEY  (or GOOGLE_API_KEY)
#   QDRANT_URL      (e.g. http://localhost:6333)
#   QDRANT_API_KEY  (optional for local Qdrant)
#   COHERE_API_KEY
#
# Optional:
#   BLOG_URL        (URL to scrape for RAG content)
#   QUESTION        (research question to answer)
#   QDRANT_COLLECTION

# Neatlogs SDK + Google GenAI instrumentation
neatlogs[google-genai]==1.1.9

# Google GenAI SDK + LangChain integration
google-genai>=1.55.0,<2.0.0
langchain-google-genai>=2.0.0,<3.0.0

# LangGraph + LangChain ecosystem
langgraph>=1.0.4,<2.0.0
langchain-core>=0.3.0,<1.0.0
langchain-community>=0.4.1,<1.0.0
langchain-classic>=1.0.0,<2.0.0

# OpenAI embeddings (used alongside Gemini for embeddings)
langchain-openai>=1.1.3,<2.0.0
openai>=1.0.0,<2.0.0

# Qdrant vector store
# Pin below 1.16 — langchain-community uses QdrantClient.search() which
# was replaced by query_points() in qdrant-client>=1.16
qdrant-client>=1.7.0,<1.16
langchain-qdrant>=0.1.0,<1.0.0

# Cohere reranker
cohere>=5.0.0,<6.0.0
langchain-cohere>=0.3.0,<1.0.0

# Utilities
pydantic>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
