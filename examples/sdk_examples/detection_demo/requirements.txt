# Detection Demo - Multi-Framework Workflows
# ============================================
# Workflows: Customer Support (LangGraph), Content Moderation (CrewAI),
#            Research Assistant (LangChain), Sales Qualification (LangGraph)
# All workflows use Azure OpenAI via AzureChatOpenAI.
#
# Required env vars (.env):
#   NEATLOGS_API_KEY, NEATLOGS_ENDPOINT
#   AZURE_OPENAI_API_KEY, AZURE_OPENAI_ENDPOINT
#   AZURE_OPENAI_DEPLOYMENT_NAME, AZURE_OPENAI_API_VERSION

# Neatlogs SDK + Azure AI Inference instrumentation
neatlogs[azure-ai-inference]==1.1.9

# LangChain ecosystem (Workflows 1, 3, 4 + base for CrewAI)
langchain>=1.1.2,<2.0.0
langchain-classic>=1.0.0,<2.0.0
langchain-core>=0.3.0,<3.0.0
langchain-community>=0.4.1,<1.0.0
langchain-openai>=1.1.3,<2.0.0

# LangGraph (Workflows 1, 4)
langgraph>=1.0.4,<2.0.0

# CrewAI (Workflow 2: Content Moderation)
crewai[azure-ai-inference]==1.5.0

# Utilities
pydantic>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
